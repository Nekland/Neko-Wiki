{% extends 'NekoWiki::page_layout.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-12">

            <h1>{{ page.title }}</h1>

            {{ page.content|markdown }}
        </div>
    </div>

{% endblock body %}
